<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.edu.erp.mapper.SysRoleMapper">
    <select id="findRolesByUserId" parameterType="long" resultType="com.edu.erp.domain.SysRole">
        select tsr.id, tsr.role_code as roleCode, tsr.role_name as roleName
        from t_sys_role tsr, t_sys_user_role_ref tsurr
        where tsr.id = tsurr.role_id
          and tsr.status = 1
          and tsurr.user_id = #{userId}
    </select>
</mapper>